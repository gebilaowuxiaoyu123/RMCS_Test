# 算法组考核

> 本文件夹存放**算法组培训考核**方向的作业，与往期周任务（`docs/zh-cn/第二周/`、`第三周/`）分开。

## 任务来源

《2027赛季算法组培训考核作业细则》（飞书云文档）

| 方向 | 状态 | 文档 |
|---|---|---|
| 控制方向（§1 龙门架 / §1.2 云台辨识） | — | 见 `rmcs_motor_demo/` 与 `docs/zh-cn/第二周/` |
| 视觉方向（§2） | — | — |
| **导航方向（§3）** | 🚧 进行中 | [导航方向.md](导航方向.md) |

## 导航方向（§3）

**目标**：使用 `rmcs-navigation-deps` 控制哨兵，挑选一个全局规划器
（不能用现在的 `NavfnPlanner`），做到在实际场地的跨场导航。

| 文档 | 用途 |
|---|---|
| [导航方向.md](导航方向.md) | **作业正文**：任务解读、基线确认、系统理解、选型论证、调参方案、验证方法、留痕与风险 |
| [任务规划.md](任务规划.md) | **行动指南**：认知准备、环境准备、分阶段执行计划、命令速查、检查清单 |

## 当前进展

| 项 | 状态 |
|---|---|
| 依赖栈拉取（`rmcs-navigation-deps` + 6 子模块） | ✅ |
| 基线确认（现用 `NavfnPlanner`，细则表述正确） | ✅ |
| 地图确认（`rmuc-v2.png`，292×161 px @ 0.1 m ≈ 29.2×16.1 m） | ✅ |
| fork + 工作分支（`feat/smac-global-planner`，基于上游 `main`） | ✅ |
| 运行时依赖（`nav2-mppi-controller`、`py-trees`） | ✅ |
| 构建 + 静态测试栈启动验证 | ✅ |
| 基线录屏 → 换 `SmacPlanner2D` → 调参 | ⏳ 进行中 |
| 跨场验证与最终提交 | ⏳ 待做 |

## 注意

- 代码改动提交在 **fork 的 `rmcs-navigation`** 仓库，不在本作业仓（详见 [导航方向.md](导航方向.md) §7）。
- 依赖栈 `rmcs_ws/src/rmcs-navigation-deps/` 是第三方代码，**不入本仓库**（已加 `.gitignore`）。
